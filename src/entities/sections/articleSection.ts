/**
 * A file appears!
 */
import { Markdown } from '../content/markdown'

import { Section } from '.'

export class ArticleSection extends Section<Markdown> {
  populate (): void {
    // @TODO
  }
}
